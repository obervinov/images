---
name: Release

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  changelog:
    uses: obervinov/_templates/.github/workflows/changelog.yaml@v2.0.2

  # readme:
  #   uses: obervinov/_templates/.github/workflows/readme.yaml@v1.2.8

  images:
    uses: obervinov/_templates/.github/workflows/images.yaml@v2.0.2

  create-release:
    uses: obervinov/_templates/.github/workflows/release.yaml@v2.0.2
    needs: [changelog, images]
